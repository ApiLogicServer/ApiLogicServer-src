you cannot use defaultxx, since windows <> mac
you can add the default venv
    "python.defaultInterpreterPath": "${workspaceFolder}/../venv",
    but it's hard to clear out for test:
        settings.json -- python.venvFolders: https://code.visualstudio.com/docs/getstarted/settings#:~:text=Settings%20file%20locations,Code%2FUser%2Fsettings.json
            Windows: %APPDATA%\Code\User\settings.json
            macOS $HOME/Library/Application\ Support/Code/User/settings.json
            Linux $HOME/.config/Code/User/settings.json

        mac: cd ~/.local/share/virtualenvs

        https://stackoverflow.com/questions/69066136/how-to-remove-all-virtualenv-vscode-python


Use creating python -> python.defaultInterpreterPath
    sys.executable (except for DevEnv)
    runs    windows (y), mac , linux