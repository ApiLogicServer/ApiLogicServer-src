# GenAI-Logic runtime requirements, minus `openai`.
#
# Built for the FedEx GENAI797 / FOSS 1033497 governance review — removes the
# only bundled third-party AI vendor SDK so Nexus IQ's dependency scan shows
# zero embedded LLM SDKs, leaving Claude (already under FIRST/EnCORE review)
# as the sole AI platform touching FedEx data.
#
# Verified 2026-08-12: installs cleanly in a fresh venv (pip check: no broken
# requirements), and the demo_customs_clvs project's full logic/ tree
# (declare_logic.py + all logic_discovery/* modules) imports with zero
# errors against this environment — no openai references anywhere in that
# project. See openai-dependency-removal-proposal-2026-08-12.md for the full
# analysis, including the "AI Rules" pattern (basic_demo_ai_rules-supplier)
# which references openai but is not present in demo_customs_clvs.
#
# Source of truth: ../../requirements.txt (this file = that file minus the
# `openai==1.55.3` line). Regenerate if the source file changes:
#   grep -v -i '^openai' ../../requirements.txt > requirements-runtime-no-openai.txt

safrs>=3.1.7
PyJWT>=2.13.0
python-dateutil==2.8.2
logicbankutils==0.6.0
LogicBank>=1.32.00
inflect==5.0.2
inflection==0.5.1
python-dotenv==1.2.2
email-validator==1.1.1
cryptography>=46.0.7; platform_system != 'Windows' or platform_machine != 'ARM64'
rsa
PyMySQL>=1.1.1
oracledb==2.1.2; python_version < '3.13'
oracledb>=2.5.0; python_version >= '3.13' and (platform_system != 'Windows' or platform_machine != 'ARM64')
# pip install PyMySQL[rsa]
# fix warnings.warn("urllib3
requests>=2.33.0
chardet<6
gunicorn==22.0.0
# psycopg2-binary==2.9.5
psycopg2-binary>=2.9.5; python_version < '3.13'
psycopg[binary]>=3.1.0; python_version >= '3.13' and (platform_system != 'Windows' or platform_machine != 'ARM64')
DotMap==1.3.25
behave==1.2.6
alembic==1.7.7
python-ulid==2.7.0
psutil>=6.1.0
GeoAlchemy2==0.12.5
confluent-kafka>=2.6.0; platform_system != 'Windows' or platform_machine != 'ARM64'
faker==30.4.0
natsort==8.4.0

Flask-Login>=0.6.3
Flask-JWT-Extended>=4.7.1
flask_bcrypt==1.0.1

SQLAlchemy-Utils==0.38.2
WTForms==2.3.3

# from safrs
Flask>=3.1.3
Flask-Cors>=6.0.0
Flask-RESTful>=0.3.9
flask-restful-swagger-2>=0.35
Flask-SQLAlchemy==3.0.3
flask-swagger-ui>=4.11.1
itsdangerous>=2.2.0
Jinja2>=3.1.6
MarkupSafe==2.1.3
six==1.16.0
SQLAlchemy>=2.0.48
Werkzeug>=3.1.6
reportlab==4.2.0
xlsxwriter==3.2.0
build

astor==0.8.1
colorama==0.4.6

# transitive dependency security pins
click>=8.3.3
idna>=3.15
lxml>=6.1.0
mako>=1.3.12
pillow>=12.3.0
pyasn1>=0.6.4
urllib3>=2.7.0
