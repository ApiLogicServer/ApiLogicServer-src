# logs

Automatic logging is configured in `config/logging.yml`, so that your AI Ass't can 'see' your debug console.  That way, you don't have to keep pasting it to diagnose errors.

Server log files are written here (e.g., `als.log`).

This directory must exist at startup — it is included in the prototype so generated projects have it from the start.
