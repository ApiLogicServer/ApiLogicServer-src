# Project Creation Report

This project was created by `genai-logic create`.

- **Project name:** library_rfi
- **Database:** `sqlite:///samples/dbs/starter.sqlite`
- **Created:** September 03, 2026 18:46:20

## Scaffold

Every project starts from a scaffold - the template `create` clones and customizes.
This project's scaffold:

- **Base template:** `/Users/val/dev/genai-logic/ApiLogicServer-dev/build_and_test/genai-logic/venv/lib/python3.13/site-packages/api_logic_server_cli/prototypes/base` (always the foundation - every project starts here)
- **Overlay:** none - this project is the unmodified base template
- **Overlay (Project Context Engineering):** `/Users/val/dev/genai-logic/ApiLogicServer-dev/build_and_test/genai-logic/system/project_context_engineering` (training file additions/overrides copied into `docs/training/` - see this project's `docs/training/$readme.md` for the exact overlay timestamp/version)

The scaffold provides, out of the box:

* SQLAlchemy ORM models
* Admin Web App
* JSON:API endpoints, MCP Support, and Swagger docs
* LogicBank rules engine
* Framework wiring for security/RBAC, Kafka Message Integration, and AI Rules

See this project's root readme (`readme.md`, or `readme_standard.md` for
demo-named projects where a demo-specific readme replaces it) for what was
actually generated.

The scaffold is extensible: `--from_git=<git-url-or-directory>` overlays your own files
on top of base at creation time - see
[github.com/ApiLogicServer/scaffold-sample](https://github.com/ApiLogicServer/scaffold-sample)
for a minimal example you can clone and extend.

## Next steps

See `project_creation_prompt.md` in this folder for what was requested (inferred from
the create command, unless a real requirements prompt was supplied).

You can still add business logic at any time - say **"implement requirements"** (or
"impl req") to an AI assistant, or write rules directly in `logic/logic_discovery/`.

## Use Cases

- **loans_and_fines** — `docs/requirements/loans_and_fines/requirements.md` — 21-day
  loans with one renewal, configurable per-day fine capped per book, member fine-balance
  rollup, checkout block above a configurable unpaid-fine threshold.
- **holds** — `docs/requirements/holds/requirements.md` — FIFO hold queue for
  already-checked-out books; oldest hold marked "ready" when the book is returned.

See `docs/requirements/ad-libs.md` for judgment calls made beyond the confirmed spec
(hold "ready" vs. auto-checkout, `loan_period_days` also made configurable, etc.).

## Schema decisions (Method 4, System Creation Services)

Built on top of the `create`-generated `sys_config` (starter.sqlite), following the
Manager CE's Method 4 STEP 1b (partial interview) → STEP 4 pre-DDL analysis sequence:

- **4a Constants → `sys_config`:** `fine_rate_per_day` (0.25), `fine_cap_per_book` (10.0),
  `fine_block_threshold` (5.0), `loan_period_days` (21) — all runtime-configurable.
- **4b FK inventory:** `loan.member_id → member`, `loan.book_id → book`,
  `hold.member_id → member`, `hold.book_id → book`.
- **4c Request Pattern / 4d Type hierarchy (STI):** both confirmed not applicable — no
  AI/email/Kafka signal and no subtype phrasing in the domain.
- **Tables added:** `member`, `book`, `loan`, `hold` (DDL in
  `database/library_rfi_ddl.sql`).
- **sys_config wiring:** `member.sys_config_id` / `loan.sys_config_id` FKs with mirrored,
  `Rule.copy`-derived columns (snapshot semantics — a rate change doesn't retroactively
  alter existing loans/members).
- **Admin app:** `ui/admin/admin-merge.yaml` (generated by `rebuild-from-database`)
  swapped directly into `admin.yaml` — no prior customizations existed to protect
  (previous version kept as `admin.yaml.bak`).

## Creation Steps (actually run, in order)

1. `genai-logic create --project-name=library_rfi --db_url=sqlite:///samples/dbs/starter.sqlite`
2. STEP 1b partial interview (see `library_rfi-transcript.md`), confirmed by user
3. DDL applied: `sqlite3 library_rfi/database/db.sqlite < library_rfi/database/library_rfi_ddl.sql`
4. `cd library_rfi && genai-logic rebuild-from-database --db_url=sqlite:///database/db.sqlite && cd ..`
5. `ui/admin/admin.yaml` swapped for the rebuild-generated `admin-merge.yaml`
6. Logic written: `logic/logic_discovery/loans_and_fines.py`, `logic/logic_discovery/holds.py`
7. Seed data written and run: `cd library_rfi && PROJECT_DIR=$(pwd) python database/test_data/alp_init.py && cd ..`
   (required two environment fixes: a `sys.path` insert for `ModuleNotFoundError: No
   module named 'config'`, and creating the missing `logs/` directory)
8. Per-use-case requirements docs written (see Use Cases above)

## CE/Training Files Read (self-reported, STEP 5d)

*(files actually loaded this run, in order; no additional reads were performed solely to
produce this list)*

- `library_rfi/.github/copilot-instructions.md` — STEP 3, project CE (project header
  v3.6 + base template CE v3.38)
- `library_rfi/docs/training/logic_bank_api.md` — STEP 3, rule API reference (read in
  full twice across the session due to a context-compaction boundary)
- `library_rfi/docs/training/implement_requirements.md` — STEP 3, schema conventions
- `library_rfi/docs/training/logic_bank_patterns.md` — STEP 3, implementation patterns
- `library_rfi/docs/training/RequestObjectPattern.md` — STEP 3, integration pattern
  (determined not applicable to this domain)
- `library_rfi/CLAUDE.md` — confirms the two `@`-imports above
