This needs to be the same as: api_logic_server_cli/prototypes/base/api/expose_api_models.py

We have 2 because this one is called by discovery, so needs the required function.

Maybe we should change the discovery function to special case that (might be better than 2 versions?)


